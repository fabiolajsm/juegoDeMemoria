import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.juegoDeMemoria.pps',
  appName: 'Juego de memoria',
  webDir: 'www',
};

export default config;
